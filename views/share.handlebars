<!doctype html>

<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Share Your File</title>
    <meta name="description" content="Allows the user to share the file that they just uploaded">
    <meta name="author" content="Richard Horton">
    <link href="style.css" type="text/css" rel="stylesheet" />
</head>

<body>
    <h1>Share Your File</h1>
    <h3>Use this link to share your file with others</h3>
    {{#if message}}
    <p class="msg">{{message}}</p>
    <!-- If a message is passed to the page, display it here -->
    {{/if}}

    {{#if link}}
    <p class="linkDetails">Here is your link:</p>
    <p class="linkDetails">{{link}}</p>
    <!-- Displays the link to the user -->
    
    <div id="linkHolder">
        <a align="center" href="mailto:?subject=The file you asked for&amp;body=Here's a file I wanted to share with you {{link}}">Share by Email</a>
    </div>
    {{/if}}

    <form action="/" class="backButton" name="backBtn">
        <input type="submit" value="Back" />
    </form>

</html>